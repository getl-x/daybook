# daybook 环境变量示例（在仓库根目录执行）：复制成 .env 后改成自己的值
#   cp deploy/daybook.env.example .env
#   chmod 600 .env
#
# 注意：.env 不进仓库、不进镜像（见 .gitignore / .dockerignore）。

# —— 数据库 ——
# 给 compose 里的 postgres 容器用（也是 app 拼 DATABASE_URL 用的口令）
POSTGRES_PASSWORD=换成一个长随机串

# —— 应用 ——
# 生产环境至少 32 个字符；生成：openssl rand -base64 48
JWT_SECRET=换成一个至少 32 字符的随机串
# 容器内监听端口，默认 8090（compose 只把它绑到宿主 127.0.0.1，反代指向它）
PORT=8090
# development | test | production
NODE_ENV=production
# trace | debug | info | warn | error | silent
LOG_LEVEL=info

# —— Web Push（可选；默认无需配置）——
# 服务端首次启动会自动生成一对 VAPID 密钥并存入数据库，之后复用；通常什么都不用填。
# 只有在需要固定/迁移密钥（例如沿用旧部署的已订阅设备）时才显式配置；配置后以环境变量优先。
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# VAPID_SUBJECT=mailto:you@example.com

# —— 跨源（可选）——
# 浏览器里的 PWA 与后端同源，不需要配；Android APK（Capacitor 壳）的源是 https://localhost，
# 默认已放行，无需修改。只有在别的地方（比如另一个域名的自建页面）嵌了接口时才需要加：
# CORS_ORIGINS=https://localhost,https://diary.example.com
