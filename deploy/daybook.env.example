# daybook 环境变量示例（在仓库根目录执行）：复制成 .env 后改成自己的值
#   cp deploy/daybook.env.example .env
#   chmod 600 .env
#
# 注意：.env 不进仓库、不进镜像（见 .gitignore / .dockerignore）；
# 但它必须存在——compose.yml 的 env_file 指向它，缺文件 compose 会直接报错。
#
# 与 Node 版的差别：没了 Postgres 与自研 JWT，因此 POSTGRES_PASSWORD、
# JWT_SECRET、DATABASE_URL、NODE_ENV、LOG_LEVEL、CORS_ORIGINS 都不再被读取。
# 数据库是 PocketBase 自带的 SQLite 单文件，令牌签发由 PocketBase 负责，
# 跨源由 PocketBase 的中间件统一处理（无需配置，见文件末尾）。
#
# 下面每一项都有内置默认值，且 compose.yml 已经把容器内路径定好了，
# 所以这个文件默认可以是空的——只在需要覆盖时才取消注释。

# —— 应用 ——
# 数据库、上传文件与备份的存放目录。compose 已设为 /app/pb_data 并挂了名字卷。
#DAYBOOK_DATA_DIR=/app/pb_data
# 前端构建产物目录。compose 已设为 /app/pb_public（镜像里已铺好 dist）。
#DAYBOOK_PUBLIC_DIR=/app/pb_public
# 容器内监听地址。compose 已设为 0.0.0.0:8090，宿主只把它绑到 127.0.0.1。
#DAYBOOK_ADDR=0.0.0.0:8090
# 版本号，出现在 /healthz 的 appVersion 里，用于部署排查；建议填 git tag。
#DAYBOOK_VERSION=dev

# —— Web Push（可选）——
# VAPID 密钥对由服务端首次启动时自动生成，存入数据库的 app_settings 表，之后复用；
# 通常什么都不用配。
# 注意：目前**不支持**用环境变量注入已有密钥——Node 版的 VAPID_PUBLIC_KEY /
# VAPID_PRIVATE_KEY 在这里不生效，填了也会被忽略。
# 只有签发者标识可由环境变量给出（mailto: 或 https: URL）。
#DAYBOOK_VAPID_SUBJECT=mailto:you@example.com

# —— 跨源 ——
# 不需要配置：PWA 与后端同源；Android APK（Capacitor 壳）的源 https://localhost
# 由服务端统一放行。
