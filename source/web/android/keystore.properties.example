# Android 签名配置的模板：复制成 keystore.properties 再填自己的值。
#
# ⚠️ keystore.properties 本身**不进仓库**（.gitignore 已挡 *.jks / *.keystore / keystore.properties），
#    仓库里只留这个 .example。
#
# 本地出正式包（Android Studio 或命令行）：
#   cd source/web/android
#   cp keystore.properties.example keystore.properties     # 填成自己的值
#   ./gradlew assembleRelease
#
# 还没有 .jks 的话，先生成一个（keytool 随 JDK 一起装好）：
#   keytool -genkeypair -v -storetype PKCS12 -keystore daybook-release.jks \
#     -alias daybook -keyalg RSA -keysize 4096 -validity 10000
# 看看已有 .jks 里的 alias 与有效期（CI 需要 alias 与两个口令）：
#   keytool -list -v -keystore 你的.jks
#
# 在 GitHub 上出正式包：把 .jks 与口令存成 secret（见 docs/zh-CN/operations.md 的"发一个 APK"），
#   .github/workflows/android-release.yml 会在 runner 上自动写出这个文件；
#   没有这些 secret 时流水线自动退化成 debug 签名包。

# 绝对路径最稳妥（相对路径按 source/web/android/ 解析）
storeFile=/absolute/path/to/daybook-release.jks
storePassword=换成你的 keystore 口令
keyAlias=daybook
keyPassword=换成你的 key 口令
