# 构建镜像时忽略的东西：镜像里只要有源码 + 依赖 + 前端产物
node_modules
**/node_modules
.git
.github
.reasonix
.vscode
.idea

# 文档与示例
docs
*.md
!README.md

# 本地配置与密钥（绝不进镜像）
.env
.env.*
!.env.example
*.jks
*.keystore
keystore.properties

# 备份与测试产物
*.dump
*.sql.gz
coverage
test-results
playwright-report

# 前端构建产物：镜像里由 web-builder 阶段自己构建，
# 带上宿主机的 web/dist 会污染构建上下文（Tailwind 会扫到旧产物里的类名）
web/dist
**/dist

# 编排文件本身
compose.yml
Dockerfile
.dockerignore
