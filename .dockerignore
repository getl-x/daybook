# 白名单式 .dockerignore：除了下面显式放行的，一律不进构建上下文。
#
# 镜像里只需要「源码 + 依赖 + 前端产物」，所以文档、密钥、本地产物、备份都不上传。
# 写法与 .gitignore 保持一致（默认拒绝 + 白名单），改任一边时记得对照另一边。

# 默认忽略全部
*
!source/
!source/**

# 依赖与构建产物（镜像里自己 npm ci / vite build）
source/**/node_modules/
source/**/dist/
source/**/coverage/
source/**/test-results/
source/**/playwright-report/
source/**/.vite/
source/**/.cache/
source/**/*.tsbuildinfo

# 本地密钥（绝不进镜像）
source/**/.env
source/**/.env.*
!source/**/.env.example
source/**/*.jks
source/**/*.keystore
source/**/keystore.properties

# 备份与测试数据
backups/
*.dump
*.sql.gz
